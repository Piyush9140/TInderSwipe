/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import Swipe from './src/screen/Swipe';

AppRegistry.registerComponent(appName, () => Swipe);
